#include "fs.h"
static struct superblock sb;
void fs_init(void)
{
}
void fs_list(void)
{
}
struct file *fs_open(const char *name)
{
    return 0;
}
int fs_read(struct file *file, void *buffer, uint32_t size)
{
    return 0;
}
//filesystem stuff ig, I need to comment more on code man
